import styled from "styled-components";

export default styled.div`
  margin-top: 27px;
  & > p {
    font-size: 0.7rem;
    text-align: left;
  }
  .read-more {
    color: #2d9cdb;
    cursor: pointer;
  }

  border-bottom: 1px solid rgba(0, 0, 0, 0.2);
  padding-bottom: 28px;
`;
